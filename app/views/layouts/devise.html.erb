<!DOCTYPE html>
<html>
  <head>
    <title>Adobe Annotation Studies</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'devise' %>
  </head>
  <body>
    <div id="devise-container">
      <%= yield %>
    </div>
  </body>
</html>
